# 基于 LLM+Git 的代码作业智慧评价平台
## 需求文档（PRD）

---

## 目录
1. 项目概述
2. 需求分析
3. 系统架构设计
4. 数据库设计
5. API接口设计
6. 前端页面设计
7. 多维评价模型
8. 项目计划
9. 预算规划
10. 预期效益

---

## 1. 项目概述

### 1.1 项目背景
当前，以大型语言模型（LLMs）为代表的AI技术正深刻重塑编程教育。AI编程助手（如Copilot、ChatGPT）已演进为能提供完整方案的"虚拟结对编程伙伴"，在为学习者提效拓知的同时，也给传统教学与评价带来颠覆性挑战：

| 困境类型 | 具体表现 |
|---------|---------|
| 学术诚信冲击 | 学生直接使用AI生成"原创"代码，核心编程思维与实践能力无法锻炼 |
| 评价机制滞后 | 主流查重系统（Turnitin、MOSS）基于文本相似度检测，难以识别LLM生成的"语义等价、形式迥异"代码 |
| 教师负担激增 | 需投入大量精力甄别海量作业，挤占教学设计、个性化辅导时间 |

### 1.2 项目目标
构建一个深度整合 **LLM+Git** 技术的代码类教学评价平台，将评价视角从静态"代码结果"转向动态"人机协作过程"，建立科学的多维度能力评估体系。

### 1.3 项目基本信息

| 项目要素 | 内容 |
|---------|------|
| **项目名称** | 基于LLM+Git的代码作业智慧评价平台 |
| **项目类别** | 科技发明制作 |
| **起止时间** | 2026年3月1日 - 2027年2月27日 |
| **申报经费** | 4000元 |
| **项目负责人** | 钟小龙 |
| **指导教师** | 吴东庆（教授） |

---

## 2. 需求分析

### 2.1 用户角色分析

| 用户角色 | 角色描述 | 核心需求 |
|---------|---------|---------|
| **学生** | 修读编程课程的学习者 | 通过Git提交代码作业，获取即时、个性化的学习反馈，了解自身编程能力提升路径，查看评价详情和改进建议 |
| **教师** | 讲授编程课程的授课教师 | 配置评分规则、发布作业、查看智能评价报告，管理课程和学生，进行班级整体分析，识别AI风险作业 |
| **教学管理者** | 学院或教务处管理人员 | 查看整体学情数据，进行教学质量分析与决策支持 |

### 2.2 核心功能需求

#### 2.2.1 用户认证与权限管理模块
- 用户注册与登录（支持学生、教师、管理员角色）
- JWT令牌认证与授权
- 基于角色的访问控制（RBAC）
- 用户信息管理

#### 2.2.2 课程管理模块
- 课程创建、编辑、删除
- 课程信息展示（课程名称、描述、教师、学生列表）
- 学生选课与退课
- 课程统计信息（选课人数、作业数等）

#### 2.2.3 作业管理模块
- 作业创建、编辑、删除
- 作业基本信息配置（标题、描述、总分、截止日期）
- Git仓库配置（仓库类型、URL、分支、作业目录）
- 评价规则配置（各维度权重调整、LLM分析开关）
- 作业状态管理（进行中、已截止）

#### 2.2.4 代码智能采集模块
- 从Git仓库自动采集代码提交历史、差异等数据
- 支持GitHub、GitLab、Gitee及自定义仓库类型
- Git仓库URL验证
- 记录提交频率、代码增量、分支管理能力

#### 2.2.5 多维评价模型

| 评价维度 | 评估指标 | 权重范围 |
|---------|---------|---------|
| **正确性评分** | 自动化测试用例通过率、算法逻辑正确性 | 0-100% |
| **代码质量** | 复杂度（圈复杂度、认知复杂度）、规范性、安全性、代码重复度、技术债务 | 0-100% |
| **原创性贡献度** | 代码提交模式、代码演进过程、AI生成特征检测、代码相似度 | 0-100% |
| **过程性贡献** | 提交频率、代码增量、分支管理能力、commit message质量 | 0-100% |

#### 2.2.6 LLM智能分析模块
- 代码基础指标分析（代码行数、有效行数、注释率、复杂度）
- 代码结构分析（函数数量、类数量、条件分支、循环结构）
- LLM评价摘要生成
- 个性化改进建议生成

#### 2.2.7 代码质量分析模块（模拟SonarQube）
- 代码问题检测（TODO注释、调试打印、硬编码值、长方法、深层嵌套、重复代码等）
- 严重程度统计（blocker、critical、major、minor、info）
- 代码质量评分计算
- 技术债务估算
- 代码重复度检测

#### 2.2.8 可视化反馈模块
- 学生端：能力雷达图、多维评价分数展示、LLM分析报告、Git提交分析、改进建议列表
- 教师端：班级统计卡片、评分分布柱状图、维度分析条形图、学生评价详情表、AI风险检测列表

#### 2.2.9 AI风险检测模块
- AI生成代码特征识别
- 风险等级评估（高风险、中风险、正常）
- 风险原因分析
- 需要关注的学生列表

### 2.3 非功能需求

| 需求类型 | 描述 |
|---------|------|
| **性能需求** | 支持大规模并发代码分析，响应时间<3秒；支持至少500名学生同时使用 |
| **安全性** | JWT令牌认证，代码数据加密存储，用户权限严格控制，密码BCrypt加密 |
| **可扩展性** | 模块化设计，支持新增评价维度和分析工具，支持多种Git仓库类型扩展 |
| **易用性** | 清晰直观的用户界面，符合教学场景使用习惯，响应式布局 |
| **可靠性** | 系统稳定运行，数据备份与恢复机制 |

---

## 3. 系统架构设计

### 3.1 技术架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                     前端展示层                                       │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │   教师管理界面   │  │   学生学习界面   │  │    管理员界面    │   │
│  │  - 数据看板     │  │  - 学习看板     │  │  - 系统管理     │   │
│  │  - 课程管理     │  │  - 课程列表     │  │  - 用户管理     │   │
│  │  - 作业管理     │  │  - 作业提交     │  │  - 数据统计     │   │
│  │  - 评价分析     │  │  - 评价结果     │  │                  │   │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘   │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ RESTful API
┌──────────────────────▼──────────────────────────────────────────────┐
│                     业务逻辑层                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────┐ │
│  │  用户服务    │  │  课程服务    │  │  作业服务    │  │  认证服务│ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  Git服务     │  │  LLM分析服务 │  │ 代码质量服务 │              │
│  │  -提交历史   │  │  -代码分析   │  │  -问题检测   │              │
│  │  -仓库验证   │  │  -结构分析   │  │  -质量评分   │              │
│  │  -仓库分析   │  │  -评价生成   │  │  -债务估算   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
└──────────────────────┬──────────────────────────────────────────────┘
                       │ JPA/Hibernate
┌──────────────────────▼──────────────────────────────────────────────┐
│                     数据服务层                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    H2/MySQL数据库                              │   │
│  │  用户表 | 课程表 | 作业表 | 提交表 | 评价结果表 | 选课表      │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈选择

| 分类 | 技术 | 版本 | 说明 |
|-----|------|------|------|
| **前端框架** | Vue 3 | 3.4.x | 渐进式JavaScript框架 |
| **前端构建** | Vite | 5.x | 下一代前端构建工具 |
| **前端样式** | CSS3 | - | 纯CSS样式，无额外UI框架依赖 |
| **前端路由** | Vue Router | 4.x | Vue官方路由管理器 |
| **后端框架** | Spring Boot | 3.2.x | Java企业级应用框架 |
| **数据库** | H2/MySQL | 2.2.x/8.0+ | 内存数据库/关系型数据库 |
| **ORM框架** | Spring Data JPA | 3.2.x | 数据访问层框架 |
| **认证授权** | JWT | - | JSON Web Token认证 |
| **密码加密** | BCryptPasswordEncoder | - | Spring Security密码编码器 |
| **代码分析** | 自研分析引擎 | - | 模拟SonarQube静态分析 |
| **LLM集成** | 自定义接口 | - | 预留LLM API接口 |

---

## 4. 数据库设计

### 4.1 实体关系图（ERD）

```
┌──────────┐     ┌──────────────┐     ┌────────────┐
│   User   │────<│CourseEnrollment│>────│   Course  │
└──────────┘     └──────────────┘     └────────────┘
    │                   │                  │
    │                   │                  │
    │                   │                  ▼
    │                   │           ┌─────────────┐
    │                   │           │ Assignment  │
    │                   │           └─────────────┘
    │                   │                  │
    │                   │                  │
    │                   ▼                  ▼
    │           ┌──────────────┐  ┌─────────────────┐
    └──────────<│CodeSubmission│>─│EvaluationResult │
                └──────────────┘  └─────────────────┘
```

### 4.2 实体详细设计

#### 4.2.1 User（用户实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 用户主键ID |
| username | String | NOT NULL, UNIQUE | 用户名 |
| password | String | NOT NULL | 密码（BCrypt加密） |
| email | String | UNIQUE | 邮箱地址 |
| name | String | NOT NULL | 真实姓名 |
| role | String | NOT NULL | 角色（STUDENT/TEACHER/ADMIN） |
| createdAt | LocalDateTime | NOT NULL | 创建时间 |
| updatedAt | LocalDateTime | - | 更新时间 |

#### 4.2.2 Course（课程实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 课程主键ID |
| name | String | NOT NULL | 课程名称 |
| description | String | - | 课程描述 |
| teacherId | Long | FOREIGN KEY | 授课教师ID |
| createdAt | LocalDateTime | NOT NULL | 创建时间 |
| updatedAt | LocalDateTime | - | 更新时间 |

#### 4.2.3 CourseEnrollment（选课实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 选课记录主键ID |
| courseId | Long | FOREIGN KEY | 课程ID |
| studentId | Long | FOREIGN KEY | 学生ID |
| enrolledAt | LocalDateTime | NOT NULL | 选课时间 |

#### 4.2.4 Assignment（作业实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 作业主键ID |
| title | String | NOT NULL | 作业标题 |
| description | String | - | 作业描述 |
| courseId | Long | FOREIGN KEY | 所属课程ID |
| totalScore | Integer | NOT NULL, DEFAULT 100 | 总分 |
| testCases | Text | - | 测试用例（JSON格式） |
| gradingRules | Text | - | 评分规则（JSON格式） |
| deadline | LocalDateTime | NOT NULL | 截止日期 |
| active | Boolean | NOT NULL, DEFAULT true | 是否启用 |
| createdAt | LocalDateTime | NOT NULL | 创建时间 |
| updatedAt | LocalDateTime | - | 更新时间 |

#### 4.2.5 CodeSubmission（代码提交实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 提交记录主键ID |
| studentId | Long | FOREIGN KEY | 学生ID |
| assignmentId | Long | FOREIGN KEY | 作业ID |
| repositoryUrl | String | - | Git仓库地址 |
| gitCommitHash | String | - | 提交哈希值 |
| submitCount | Integer | NOT NULL, DEFAULT 1 | 提交次数 |
| isLatest | Boolean | NOT NULL, DEFAULT true | 是否最新提交 |
| submittedAt | LocalDateTime | NOT NULL | 提交时间 |

#### 4.2.6 EvaluationResult（评价结果实体）

| 字段名 | 类型 | 约束 | 说明 |
|-------|------|------|------|
| id | Long | PRIMARY KEY, AUTO_INCREMENT | 评价结果主键ID |
| submissionId | Long | FOREIGN KEY | 提交记录ID |
| correctnessScore | Integer | - | 正确性评分 |
| qualityScore | Integer | - | 代码质量评分 |
| originalityScore | Integer | - | 原创性评分 |
| processScore | Integer | - | 过程性评分 |
| finalScore | Double | - | 综合总分 |
| llmAnalysis | Text | - | LLM分析报告 |
| suggestions | Text | - | 改进建议（JSON格式） |
| aiRiskLevel | String | - | AI风险等级（high/medium/low） |
| aiRiskReason | String | - | AI风险原因 |
| evaluatedAt | LocalDateTime | NOT NULL | 评价时间 |

---

## 5. API接口设计

### 5.1 认证接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |

#### 5.1.1 POST /api/auth/register

**请求体：**
```json
{
  "username": "string (必填)",
  "password": "string (必填)",
  "email": "string (选填)",
  "name": "string (必填)",
  "role": "string (必填, STUDENT/TEACHER/ADMIN)"
}
```

**响应体：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "student001",
    "name": "张三",
    "role": "STUDENT"
  }
}
```

#### 5.1.2 POST /api/auth/login

**请求体：**
```json
{
  "username": "string (必填)",
  "password": "string (必填)"
}
```

**响应体：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "student001",
      "name": "张三",
      "role": "STUDENT"
    }
  }
}
```

### 5.2 课程接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/courses` | GET | 获取课程列表 |
| `/api/courses/{id}` | GET | 获取课程详情 |
| `/api/courses` | POST | 创建课程 |
| `/api/courses/{id}` | PUT | 更新课程 |
| `/api/courses/{id}` | DELETE | 删除课程 |
| `/api/courses/{id}/students` | GET | 获取课程学生列表 |

### 5.3 作业接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/assignments` | GET | 获取作业列表 |
| `/api/assignments/{id}` | GET | 获取作业详情 |
| `/api/assignments` | POST | 创建作业 |
| `/api/assignments/{id}` | PUT | 更新作业 |
| `/api/assignments/{id}` | DELETE | 删除作业 |
| `/api/assignments/{id}/submissions` | GET | 获取作业提交列表 |

### 5.4 提交接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/submissions` | GET | 获取提交列表 |
| `/api/submissions/{id}` | GET | 获取提交详情 |
| `/api/submissions` | POST | 提交作业 |
| `/api/submissions/{id}` | PUT | 更新提交 |

### 5.5 评价接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/evaluations` | GET | 获取评价列表 |
| `/api/evaluations/{id}` | GET | 获取评价详情 |
| `/api/evaluations/{id}` | POST | 生成评价结果 |

### 5.6 Git分析接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/git/submission-history` | GET | 获取学生提交历史 |
| `/api/git/repository-analysis` | GET | 获取仓库分析信息 |
| `/api/git/validate` | POST | 验证Git仓库URL |

**POST /api/git/validate 请求体：**
```json
{
  "repoUrl": "string (必填)",
  "repoType": "string (必填, github/gitlab/gitee/custom)"
}
```

**响应体：**
```json
{
  "valid": true,
  "message": ""
}
```

### 5.7 LLM分析接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/analyze/code` | POST | LLM代码分析 |
| `/api/analyze/batch` | POST | 批量分析代码 |

**POST /api/analyze/code 请求体：**
```json
{
  "code": "string (必填, 待分析代码)",
  "context": {
    "assignmentTitle": "string (选填)",
    "language": "string (选填)"
  }
}
```

**响应体：**
```json
{
  "linesOfCode": 150,
  "effectiveLines": 120,
  "commentLines": 15,
  "complexity": 8,
  "structure": {
    "functionCount": 5,
    "classCount": 2,
    "structureScore": 85
  },
  "summary": "代码共 150 行，代码规模适中，命名规范程度较高...",
  "suggestions": [
    {
      "type": "complexity",
      "priority": "medium",
      "title": "复杂度较高",
      "description": "建议将复杂函数拆分为更小的函数..."
    }
  ],
  "scores": {
    "correctness": 85,
    "quality": 82,
    "originality": 88,
    "process": 80
  }
}
```

### 5.8 代码质量分析接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/quality/analyze` | POST | 代码质量分析 |

**POST /api/quality/analyze 请求体：**
```json
{
  "code": "string (必填)",
  "language": "string (选填, 默认java)"
}
```

**响应体：**
```json
{
  "metrics": {
    "lines": 150,
    "statements": 120,
    "classes": 2,
    "methods": 5,
    "commentPercent": 10.0
  },
  "issues": [
    {
      "ruleId": "todo_comment",
      "message": "TODO或FIXME注释存在",
      "severity": "medium",
      "category": "comment",
      "description": "代码中存在未完成的TODO标记..."
    }
  ],
  "issueCount": 3,
  "severityCounts": {
    "blocker": 0,
    "critical": 0,
    "major": 1,
    "minor": 1,
    "info": 1
  },
  "qualityScore": 88,
  "technicalDebtMinutes": 20,
  "duplicationPercent": 5.0,
  "complexity": {
    "cyclomatic": 8,
    "cognitive": 15,
    "rating": "低复杂度"
  }
}
```

### 5.9 完整评价接口

| 接口路径 | HTTP方法 | 功能描述 |
|---------|---------|---------|
| `/api/evaluate/full` | POST | 生成完整评价报告 |

**POST /api/evaluate/full 请求体：**
```json
{
  "code": "string (必填)",
  "language": "string (选填)",
  "context": {
    "assignmentTitle": "string",
    "studentId": "number"
  }
}
```

**响应体：**
```json
{
  "qualityAnalysis": {...},
  "llmAnalysis": {...},
  "overallScore": 85
}
```

---

## 6. 前端页面设计

### 6.1 页面结构

| 页面路径 | 角色权限 | 功能描述 |
|---------|---------|---------|
| `/` | 公共 | 登录页面 |
| `/register` | 公共 | 注册页面 |
| `/student` | STUDENT | 学生学习看板 |
| `/student/courses` | STUDENT | 我的课程列表 |
| `/student/submissions` | STUDENT | 作业提交页面 |
| `/student/results` | STUDENT | 评价结果页面 |
| `/teacher` | TEACHER/ADMIN | 教师数据看板 |
| `/teacher/courses` | TEACHER/ADMIN | 课程管理页面 |
| `/teacher/assignments` | TEACHER/ADMIN | 作业管理页面 |
| `/teacher/evaluations` | TEACHER/ADMIN | 评价分析页面 |

### 6.2 登录页面（/）

**功能模块：**
- 用户名输入框（原生input）
- 密码输入框（原生input，type=password）
- 登录按钮
- 注册链接
- 错误提示显示

### 6.3 学生学习看板（/student）

**功能模块：**
- 课程统计卡片（进行中课程数、待提交作业数、已完成作业数、平均成绩）
- 近期作业列表（作业名称、课程、截止日期、状态）
- 能力概览（各维度平均分）

### 6.4 学生作业提交页面（/student/submissions）

**功能模块：**
- 课程筛选下拉框
- 作业列表（卡片式展示）
- Git提交表单（仓库地址、分支、提交说明）
- 提交状态显示（已提交/未提交）
- 重新提交功能

### 6.5 学生评价结果页面（/student/results）

**功能模块：**
- 能力雷达图（最近5次作业平均值）
- 详细评价结果列表（卡片式）
  - 作业信息和最终得分
  - 多维评价分数（正确性、代码质量、原创性、过程性）
  - LLM智能分析报告
  - Git提交记录分析
  - 改进建议列表
- 查看代码详情按钮
- 下载报告按钮

### 6.6 教师数据看板（/teacher）

**功能模块：**
- 统计卡片（课程数、学生数、待批改作业数、平均成绩）
- 课程列表
- 作业提交统计
- 学生表现趋势

### 6.7 教师作业管理页面（/teacher/assignments）

**功能模块：**
- 课程筛选下拉框
- 作业列表表格（标题、课程、总分、截止日期、Git配置、提交人数、状态）
- 新建/编辑作业模态框
  - 基本信息（标题、课程、总分、截止日期、描述）
  - Git仓库配置（仓库类型、URL、分支、作业目录、启用开关）
  - 评价规则配置（各维度权重滑块、LLM分析开关）
- Git配置详情模态框
- 评价规则详情模态框

### 6.8 教师评价分析页面（/teacher/evaluations）

**功能模块：**
- 课程和作业筛选下拉框
- 班级整体分析卡片（提交率、平均分、优秀率、待改进率）
- 评分分布柱状图（按分数段统计）
- 维度分析条形图（各维度平均分）
- 学生评价详情表格（学生信息、各维度分数、总分、AI检测）
- AI风险检测区域（高风险学生卡片列表）

---

## 7. 多维评价模型

### 7.1 评价维度说明

#### 7.1.1 正确性评分（Correctness）
- **评估指标**：自动化测试用例通过率、算法逻辑正确性、边界情况处理
- **评分方法**：基于预设测试用例的执行结果，通过测试用例数/总测试用例数 × 100
- **权重范围**：建议30%-50%

#### 7.1.2 代码质量评分（Quality）
- **评估指标**：
  - 圈复杂度（Cyclomatic Complexity）
  - 认知复杂度（Cognitive Complexity）
  - 代码规范度（命名规范、注释率、格式规范）
  - 代码重复度
  - 技术债务估算
- **评分方法**：综合代码问题严重程度扣分 + 代码规范度加分
- **权重范围**：建议20%-35%

#### 7.1.3 原创性评分（Originality）
- **评估指标**：
  - 代码提交模式（提交频率、增量大小、commit message质量）
  - AI生成特征检测（代码结构模式、变量命名模式、代码风格一致性）
  - 代码演进过程（从初稿到终稿的修改轨迹）
- **评分方法**：基于代码特征分析和提交历史模式识别
- **权重范围**：建议15%-25%

#### 7.1.4 过程性评分（Process）
- **评估指标**：
  - 提交频率（单位时间内提交次数）
  - 代码增量（每次提交的代码变更量）
  - 分支管理能力（是否合理使用分支、合并策略）
  - Commit message质量（是否清晰描述变更内容）
- **评分方法**：基于Git提交历史数据分析
- **权重范围**：建议10%-20%

### 7.2 综合评分计算公式

```
综合总分 = 正确性评分 × 正确性权重 + 
           代码质量评分 × 代码质量权重 + 
           原创性评分 × 原创性权重 + 
           过程性评分 × 过程性权重

其中：正确性权重 + 代码质量权重 + 原创性权重 + 过程性权重 = 100%
```

### 7.3 AI风险检测模型

| 风险等级 | 判定条件 | 处理建议 |
|---------|---------|---------|
| **高风险** | 代码结构异常规整、命名模式符合AI特征、提交历史异常（一次性提交全部代码） | 人工复核代码、与学生沟通 |
| **中风险** | 部分代码段存在AI生成特征、提交频率较低但代码质量异常高 | 标记关注、结合其他维度综合判断 |
| **正常** | 代码结构符合人工编写特征、有正常的提交历史和修改轨迹 | 正常评价流程 |

---

## 8. 项目计划

### 8.1 进度安排

| 阶段 | 时间 | 任务内容 | 交付物 |
|-----|------|---------|-------|
| **第一阶段** | 2026.3-2026.6 | 需求调研与系统设计 | 《平台需求规格说明书》、系统架构设计、数据库设计、API接口设计 |
| **第二阶段** | 2026.7-2026.10 | 核心功能开发 | MVP最小可行产品（用户认证、课程管理、作业管理、代码提交、基础评价功能） |
| **第三阶段** | 2026.11-2026.12 | 测试与试点 | 测试报告、试点反馈优化、LLM分析模块完善、代码质量分析模块完善 |
| **第四阶段** | 2027.1-2027.2 | 实证研究与结题 | 实证报告、学术论文、成果推广、完整文档资料 |

### 8.2 成果形式

1. **平台产品**：集PC端教师管理界面与学生学习界面于一体的代码作业智慧评价平台
2. **知识产权**：申请1项软件著作权
3. **文档资料**：完整用户手册、技术文档、源代码
4. **学术成果**：实证研究报告或学术论文

---

## 9. 预算规划

| 预算项目 | 金额（元） | 说明 |
|---------|-----------|------|
| 服务器空间 | 500 | 云服务器租赁 |
| 网络域名 | 500 | 域名注册与备案 |
| 学习资料 | 500 | 技术书籍、文献数据库 |
| 调研交通 | 1000 | 调研、会议差旅 |
| 开发工具与云服务 | 1500 | 开发许可证、API调用等 |
| **合计** | **4000** | |

---

## 10. 预期效益

### 10.1 教育效益
- 提升编程教育评价的科学性与公平性
- 为学生提供即时、个性化的学习反馈
- 推动教育评价从"结果判定"向"能力培养"转型
- 有效识别AI生成代码，维护学术诚信

### 10.2 社会效益
- 减轻教师重复劳动负担，促进因材施教
- 推动产教融合，培养符合产业需求的工程实践人才
- 开源模式推广，促进教育资源普惠共享

### 10.3 技术效益
- 构建"教育智能"驱动的自动化评价流水线
- 形成可复制、可扩展的教育技术解决方案
- 探索LLM在编程教育评价中的创新应用模式

---

**文档版本**：V2.0  
**编制日期**：2026年6月  
**编制单位**：数学与数据科学学院  
**项目编号**：[待分配]